	 <div>
	 <ul class="layers">
          <li ng-repeat="layer in layers | filter:query | orderBy:orderProp" class="thumbnail">
            <a href="#/layers/{{layer.id}}" class="thumb"><img ng-src="{{layer.url}}"></a>
            <a href="#/layers/{{layer.id}}">{{layer.name}}</a>
            <p>{{layer.snippet}}</p>
          </li>
        </ul>
        </div>



